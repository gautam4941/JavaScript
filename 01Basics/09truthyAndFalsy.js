/*
undefined
Null
0
''
NaN
*/

var user ;
console.log( `User = ${user} and typeof( user ) = ${ typeof( user ) }` ) ;

var user = null ;
console.log( `User = ${user} and typeof( user ) = ${ typeof( user ) }` ) ;

var user = 0 ;
console.log( `User = ${user} and typeof( user ) = ${ typeof( user ) }` ) ;

var user = '' ;
console.log( `User = ${user} and typeof( user ) = ${ typeof( user ) }` ) ;

var user = NaN ;
console.log( `User = ${user} and typeof( user ) = ${ typeof( user ) }` ) ;