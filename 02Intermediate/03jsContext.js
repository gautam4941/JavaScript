name = 'Gautam' ;

console.log(  `name == name = ${ name == name }` ) ;
console.log(  `name === name = ${ name === name }` ) ;
console.log() ;
//window is not defined -> Because we are running our code using node not HTML
console.log( `name == window.name = ${ name == window.name }` ) ;
console.log(  `name == window.name = ${ name == window.name }` ) ;